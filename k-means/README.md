# Input
```txt
Make a simple interactive visualization of the K-means clustering algorithm, where I can adjust K, add my own data points but also random data, and add a toggle to switch to its K-medoids variant.
```